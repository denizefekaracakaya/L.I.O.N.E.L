"""Evaluation harnesses.  ADR-0021, ADR-0041.

Not part of the installed package (`pyproject.toml`'s `[tool.hatch.build.targets.wheel]`
packages only `src/lionel`) — this is a development- and CI-time tool, the same category
`ci/` and `scripts/` already occupy, not product code that ships.
"""
